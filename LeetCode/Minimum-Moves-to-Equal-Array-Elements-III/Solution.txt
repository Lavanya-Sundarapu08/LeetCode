class Solution {
public:
    int minMoves(vector<int>& nums) {
        int cnt = 0;
        sort(nums.begin(),nums.end());
        int maxi = nums.back();
        for(int i=0;i<nums.size();++i){
            cnt +=  maxi - nums[i] ;
        }return cnt;
    }
};